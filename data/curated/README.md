# Curated Data

This folder stores human-review-ready or product-seed datasets.

Primary Phase 1 output:

- `cafes.seed.jsonl`

Every curated cafe record must include at least one source URL, confidence notes, and `last_verified_at`.

